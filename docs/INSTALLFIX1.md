# 3.0.0 安装兼容修订

日期：2026-09-23。

原始HND包的MD5为 `ceeff7125cdf1d74b69f2a9660215c9b`，与安装失败日志一致。软件中心在执行插件脚本前，对完整 `install.sh` 扫描 `detect_package` 与 `ks_tar_install` 两个字符串，包含注释。原包第44行注释引用了宿主安装器文件名，触发了这道检查。

插件安装器没有修改软件中心本体脚本的操作。写入对象为Tailscale自己的程序、页面、资源、启动钩子、状态目录和插件注册键。本次修订仅把该行注释改为普通描述；软件中心的检查规则和保护行为保持原状。

## 验证

- 六个最终修订归档均通过相同的完整文本关键词检查。
- 与原始归档逐文件比较，仅 `install.sh` 和 `manifest.sha256` 变化；核心、辅助程序、签名清单和公钥逐字节一致。
- 打包测试13项通过；新增反例确保注释或代码里出现宿主禁止字符串仍会被拒绝。
- 安装测试21项在本机和隔离Docker中的BusyBox1.25.1通过；每次模拟安装先执行宿主关键词检查。
- 本次没有对路由器写入、安装或重启，实机安装结果仍需实际上传后确认。

修订包位于工作目录 `dist/installfix1/`，文件名带 `_installfix1`，版本字段仍为3.0.0。HND修订包MD5为 `6ede9ad4c1cdfbbce5f0478ed444ef19`；SHA-256以同目录清单为准。

构建命令：

```sh
python3 tools/package.py --revision installfix1 --output ../dist/installfix1
```
